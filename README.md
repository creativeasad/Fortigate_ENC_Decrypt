# Fortigate_ENC_Decrypt
#This script decrypts Fortigate ENC Password
#To quickly start use the following steps.
#After you have installed Python, create python virtual environment by executing the following command
python -m venv test
# Now Activate the python Virtual Environment
cd test/Scripts
Activate
#We need to install a package called pycryptodome
pip install pycryptodome
#now you're ready to execute the script
#paste the fgt.py inside the test folder
python fgt.py
